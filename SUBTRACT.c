#include <stdio.h>

int main()
{
    int num1, num2;

    printf("Select any two numbers: ");
    scanf("%d %d", &num1, &num2);

    printf("subtraction of numbers is %d\n", num1 - num2);
    return 0;
}